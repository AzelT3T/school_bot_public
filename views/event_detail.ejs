<!DOCTYPE html>
<html>

<head>
    <title>イベント詳細</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            font-size: 15px;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
            font-family: 'Roboto', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        button {
            background-color: #007BFF;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        button:hover {
            background-color: #0056b3;
        }

        button:active {
            background-color: #004391;
        }

        h1,
        h2 {
            color: #333;
        }

        .event-detail {
            margin-top: 20px;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            width: 90%;
            max-width: 500px;
            padding: 20px;
            box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.1);
        }

        .event-participants {
            list-style-type: none;
            padding: 0;
            margin-top: 20px;
        }

        .event-participants li {
            padding: 10px;
            border-bottom: 1px solid #ddd;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .event-participants li:last-child {
            border-bottom: none;
        }

        .event-participants li {
            padding: 10px;
            border-bottom: 1px solid #ddd;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .participation-status {
            color: #007BFF;
            font-weight: bold;
        }

        .participation-separator {
            width: 100%;
            height: 1px;
            background-color: #ddd;
            margin: 10px 0;
        }

        @media (max-width: 800px) {

            h1,
            h2 {
                font-size: 1.2em;
            }

            p {
                font-size: 0.9em;
            }

            .event-detail {
                margin: 10px;
                width: calc(100% - 20px);
            }
        }
    </style>
    </style>
</head>

<body>
    <div class="event-detail">
        <h2>
            イベント概要
        </h2>
        <p>
            イベント名:<%= event.event_name %>
        </p>
        <p>
            イベント詳細:<%= event.event_detail %>
        </p>
        <p>
            <a href="/event_payment/<%= event.event_id %>">参加者の支払い状況を見る</a>
        </p>
        <h2>イベント参加状況</h2>
        <ul class="event-participants">
            <% let previousStatus='' ; %>
                <% participants.forEach(participant=> { %>
                    <% if (previousStatus !=='' && previousStatus !==participant.participation_status) { %>
                        <div class="participation-separator"></div>
                        <% } %>
                            <li>
                                <span>
                                    <%= participant.name %>
                                </span>
                                <span class="participation-status">
                                    <%= participant.participation_status %>
                                </span>
                            </li>
                            <% previousStatus=participant.participation_status; %>
                                <% }) %>
        </ul>
    </div>
</body>
</html>